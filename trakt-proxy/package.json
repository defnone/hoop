{
  "name": "tmdb-proxy",
  "version": "0.8.0",
  "scripts": {
    "dev": "wrangler dev --config wrangler.tmdb.jsonc",
    "deploy": "wrangler deploy --minify --config wrangler.tmdb.jsonc",
    "test": "bunx vitest --coverage",
    "typecheck": "bunx tsc --noEmit --strict -p tsconfig.json"
  },
  "dependencies": {
    "@hono-rate-limiter/cloudflare": "^0.2.2",
    "hono": "4.13.1",
    "hono-rate-limiter": "^0.5.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260317.1",
    "wrangler": "^4.77.0"
  }
}
